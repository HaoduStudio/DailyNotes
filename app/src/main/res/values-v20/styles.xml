<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppThemeDontRinght" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowAnimationStyle">@null</item>
        <!--应小天才要求取消屏蔽右滑返回-->
        <!--<item name="android:windowSwipeToDismiss">false</item>-->

        <item name="android:hardwareAccelerated">false</item>
        <item name="android:largeHeap">true</item>
    </style>

    <style name="AppTheme.StartingWindowTheme" parent="AppTheme">
        <!-- 可以设置成纯颜色（设置一个和Activity UI相似的背景） -->
        <!--<item name="android:windowBackground">@color/startingwindow_bgcolor</item>-->
        <!--也可以设置成一张图片 -->
        <item name="android:windowBackground">@drawable/startingwindow_bg</item>
        <item name="android:hardwareAccelerated">false</item>
        <item name="android:largeHeap">true</item>
    </style>

    <style name="DialogActivityTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowFrame">@null</item> <!-- 无windowFrame -->
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowSwipeToDismiss">false</item>
        <item name="android:windowBackground">@color/selected</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>
</resources>